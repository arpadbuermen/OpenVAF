# list_pool

A pool allocator for linked list structures used in OpenVAF's MIR representation.

This crate provides efficient memory allocation for list-based data structures, optimizing for the common patterns in compiler intermediate representations.

## Usage

This is an internal library used by OpenVAF's MIR crates. See the main [README](../../README.md) for project overview.
